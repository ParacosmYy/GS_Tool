# DEV-4 History / Runtime

**作者：** AI Token Tracker Engineering Team  
**维护者：** Project Owner  
**职责：** 历史记录、CSV 导出、响应式、ARIA、性能预算和根目录启动体验。

## 验收

- 检查 320px、768px、1024px、1440px 四档布局和键盘操作。
- 导出只返回当前授权范围；管理员导出必须写入审计事件。
- 根目录 `start.bat` 始终可双击，实际服务代码位于 `windows/`。
- 动效降级不能隐藏核心数据和操作入口。

