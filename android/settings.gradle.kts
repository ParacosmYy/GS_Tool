// Author: AI Token Tracker Engineering Team
// Maintainer: Project Owner
// Purpose: Resolve Android plugins/dependencies from approved repositories only.

import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "AI Token Tracker Android"
include(":app")
