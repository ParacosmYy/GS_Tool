# Specs Template

> 用途：任何超过单点修复的 Agent 迭代，先写 Specs，再执行。Specs 是执行边界，不是事后总结。

## 1. 标题

`<PRD编号或任务编号> - <短标题>`

## 2. 目标

- 本轮要达成什么。
- 用户可观察到什么变化。
- 哪些文件或模块是主要交付物。
- 本轮三轴目标：工程 `E?`，用户 `U?`，设备 `D?`。
- 本轮完成后必须能用哪条命令或人工检查证明。

## 3. 非目标

- 本轮明确不做什么。
- 哪些已有问题只记录，不顺手扩大。
- 哪些模块禁止触碰。
- 哪些三轴状态明确不提升，例如“不提升设备状态，真实硬件仍未验证”。

## 4. 约束

- `CLAUDE.md`
- `docs/constraints/01-project-overview.md`
- 按任务类型补充 `03-architecture.md`、`04-coding-standard.md`、`05-ui-standard.md`、`07-directory-structure.md` 或 `docs/serial_station_architecture.md`
- 运行时、测试、打包统一通过 `uv run ...` 脚本入口。
- 用户已有改动不得回退。
- 新增源码必须落在 canonical Python 包路径，并有 pytest 或启动烟测覆盖。
- 涉及外设能力时，设备验证状态不得无证据提升。

## 5. 改动范围

| 范围 | 路径 | 允许动作 |
|------|------|----------|
| 主要文件 | `<path>` | 新增 / 修改 / 删除 |
| 只读参考 | `<path>` | 只读 |
| 禁止修改 | `<path>` | 禁止 |

## 6. 验收标准（先写标准再执行）

- [ ] 功能、文档或工具结果满足 PRD。
- [ ] 工程状态达到 `<E?>`，证据：`<命令/文件/测试>`。
- [ ] 用户状态达到 `<U?>`，证据：`<入口/截图/人工路径/说明>`。
- [ ] 设备状态达到 `<D?>`，证据：`<单测/fake/虚拟设备/真实硬件记录>`。
- [ ] 测试、启动或打包命令通过：`<命令>`。
- [ ] `EmbedDebug.bat` 启动链路在受影响时已验证。
- [ ] 没有恢复遗留原生工程线或新增平行 native 构建目录。
- [ ] 用户已有改动没有被回退。
- [ ] 本轮结束有 commit；无法 commit 时写明具体阻塞。

## 7. 失败条件

- 出现无法解释的构建失败。
- 需要修改禁止文件。
- 子任务之间发生同文件冲突。
- 验证命令不能覆盖本轮核心行为。
- 实际改动需要扩大到非目标范围。
- 无法提供三轴状态证据。
- GO 循环触发 20 轮或 30 分钟安全刹车。

## 8. GO 配置

GO 只执行已经写入 Specs 的命令，不自行扩大任务范围。循环顺序固定为：

```text
execute -> check -> fix -> 下一轮
```

安全刹车：

- `max_rounds` 最大 20。
- `max_minutes` 最大 30。
- 任意刹车触发后停止，输出最后失败命令和错误摘要，转入 LOOP。

```json
{
  "max_rounds": 20,
  "max_minutes": 30,
  "execute": [
    "uv run test-embeddebug-py"
  ],
  "check": [
    "git status --short"
  ],
  "fix": []
}
```

## 9. BATCH 判定

- 是否需要 BATCH：是 / 否。
- 若需要，子任务数量：5-30。
- 并行度上限：1 / 3 / 6。
- 人工审查状态：未审查 / 已审查。
- 共享文件锁：`<pyproject.toml/uv.lock/README/QSS/MainWindow/PanelManager/接口/启动脚本>`
- 子任务是否文件互不重叠：是 / 否。

## 10. LOOP 路由

- Doctor：系统体检、环境、构建、启动。
- Debug：可复现 Bug、崩溃、逻辑错误。
- Simplify：清理重复、缩小复杂度、拆分过大文件。

触发条件：

| 失败表现 | 路由 | 必须产出 |
|----------|------|----------|
| 工具链、uv、PyQt、PyInstaller、启动脚本异常 | Doctor | 诊断输出或 `docs/reviews/debug/` 报告 |
| 可复现崩溃、编译错误、逻辑错误 | Debug | 复现命令、错误摘要、候选文件、回归命令 |
| 文件过大、重复实现、目录分叉、职责混杂 | Simplify | 只读扫描报告和下一步技术债 Specs |

## 11. 收口记录

| 项 | 结果 |
|----|------|
| 工程状态 | `<E?> -> <E?>` |
| 用户状态 | `<U?> -> <U?>` |
| 设备状态 | `<D?> -> <D?>` |
| 验证命令 | `<命令和结论>` |
| commit | `<hash 或阻塞原因>` |
