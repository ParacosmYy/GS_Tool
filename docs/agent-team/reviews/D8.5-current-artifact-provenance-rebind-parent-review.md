# D8.5 / D9 current-artifact provenance rebind parent review

| Field | Value |
|---|---|
| Hook | `after-design` + `after-source-change` |
| Scope | Live artifact identity wording in `docs/agent-team/delivery-register.json` and `docs/agent-team/acceptance.json` |
| Owner | Architect (parent) |
| Route | Fixed Luna/max role review plus independent Luna/max read-only review |
| Result | `accepted-with-limits` after parent correction |

## User outcome

The delivery ledger no longer presents old package captures as the current
candidate. Historical hashes, sizes, and source snapshots remain available for
their original slices, while the D9 aggregate points to the current candidate
recorded by the manifest, release dossier, human handoff, and UI-11 evidence.

The D6.2 38 MB binary capture is also identified as `dist/QuillForge.exe`;
`dist/QuillForge.release.json` remains the small manifest path rather than the
binary object.

## Architecture and provenance decision

This is a documentation/evidence-boundary correction. The canonical current
identity remains the projection of:

- `dist/QuillForge.release.json`;
- `docs/release/handoff-2026-08-09.json`;
- `docs/RELEASE_HANDOFF.md`; and
- the current UI-11/D9 aggregate evidence.

The change does not introduce a second artifact registry or rewrite historical
reports. It changes only stale labels such as `current package` or `current
artifact` to explicit historical-capture wording, keeps the captured values,
and corrects the D6.2 path/object mismatch. Statuses, limits, outcomes, and
normative `current artifact` requirements were not changed.

## Fixed-role inputs

- Project Manager / Franklin: identified stale current labels across D6.7,
  D7.3/D7.4, D8.1/D8.6, D9, and governance evidence; recommended preserving
  old captures and correcting only semantics.
- Product / Gibbs: confirmed the user/release conclusion is unchanged;
  enterprise release remains no-go and the three stale-report failures plus
  ten external gates remain open.
- Developer 1 / Ampere: confirmed there is no structured current-artifact
  field in the live ledgers; recommended the smallest text-only rebind and a
  current D9 aggregate projection.
- Developer 2 / Hegel: independently confirmed the physical root/dist package
  provenance and the historical-label drift in D7/D8/D9 records.
- QA / Kepler: authorized JSON, hash, manifest, handoff, static-check, and
  release-verifier evidence only; prohibited Qt/EXE launch, runtime refresh,
  tests, clean-machine, and cross-machine operations.

## Independent review and parent action

An independent Luna/max review was assigned to Aristotle with read-only scope
over the two live ledgers. The review window was time-boxed and shut down
without a conclusion, so no child PASS is claimed. The parent therefore relied
on the fixed-role findings, direct JSON parsing, and the authorized static
identity checks below; no runtime or test claim is inferred from the absent
independent conclusion.

## Simplification assessment

The safe simplification is limited to terminology and one path correction. No
JSON schema refactor, automatic current-artifact resolver, report regeneration,
or broad historical rewrite was judged safe in this slice. Keeping the existing
evidence arrays and adding explicit historical wording preserves traceability
with the smallest behavioral surface.

## Public-source applicability

No manufacturer or embedded public-source requirement applies. This slice is
release-ledger documentation for a Python/PyQt6/PowerShell desktop project;
it does not modify embedded C/C++, MCU, BSP/HAL/CMSIS, RTOS, ISR/DMA, driver,
protocol, bootloader, Flash/NVM, power, or motor-control code. No MISRA,
ISO 26262, certification, legal-clearance, or vendor-compliance claim is made.

## Authorized non-destructive verification

- Live JSON parse — PASS for both ledgers, the handoff index, manifest, and
  release dossier.
- Read-only root/dist identity comparison — PASS: both files are
  `8AF11EE8D661FDFF2A564DE6F48D7056D3A41887A53B969AA5CAF99AE5B115E6` and
  `38,365,225` bytes, matching the manifest and dossier.
- `scripts/verify_handoff.ps1` — PASS.
- `scripts/check.ps1` — PASS.
- Parent-owned `scripts/verify_release_handoff.ps1` — expected NO-GO with the
  same three stale-runtime mechanical failures and ten open release gates.

Not authorized: launching `QuillForge.exe`, instantiating QApplication or Qt
windows, refreshing interactive/performance reports, running unit tests or
test-only assets, changing the package, or performing external release gates.

## Evidence and limits

The current candidate remains root/dist SHA-256
`8AF11EE8D661FDFF2A564DE6F48D7056D3A41887A53B969AA5CAF99AE5B115E6`,
`38,365,225` bytes, with source snapshot
`tree-sha256:87abe606819cd85810d11b4c1849ad5a6940a590a381b7131fb117990ac97866`.
The release dossier remains `no-go`; current package identity does not refresh
the stale packaged/interactive/preflight reports or close signing, installer,
update, file-association, support, clean-machine, legal, pressure,
hard-power, or cross-machine gates.

## Disposition

`accepted-with-limits`: live ledger semantics are corrected and historical
evidence is preserved. This is not a runtime, enterprise-release, or
cross-machine completion claim.
