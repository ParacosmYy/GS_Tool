# D7.4 static criteria closure review

- **Date:** 2026-08-09
- **Delivery:** D7 / D7.4 bounded asynchronous workspace search
- **Owner:** Architect (parent)
- **Disposition:** D74-AC01..03 ACCEPTED WITH LIMITS; D74-AC04 remains
  `in-progress` because current packaged evidence is stale and interactive
  search remains unrun

## Criteria audit

### D74-AC01 — bounded immutable search contract

`PASS WITH LIMITS`. The application query/result DTOs, explicit resolved root,
deterministic bounded directory sample, regular-file and symlink/reparse
boundary, and provider/application seam are present in the current source.
The recorded source boundary smoke covers deterministic traversal,
UTF-8/UTF-16, binary/exclusion/symlink behavior, and provider path validation.

The criterion remains limited to literal line-local search. It does not claim
regex, indexing, remote roots, cross-file replacement, or large-file support.

### D74-AC02 — bounded filesystem workload and diagnostics

`PASS WITH LIMITS`. The current policy and provider enforce directory, file,
total-byte, per-file, result, depth, line, and issue bounds; unsupported input
is skipped or diagnosed; incomplete results carry explicit limit state; and
the diagnostic projection is bounded and read-only. Existing source/offscreen
and D7.4.1 diagnostic evidence supports the contract.

The checked-in packaged report is historical and lacks the newer artifact
identity fields; it is not used as current packaged proof. Clean-machine,
cross-machine, permission/disk-pressure, and interactive evidence remain open.

### D74-AC03 — cancellation and stale-result safety

`PASS WITH LIMITS`. The TaskRunner operation/generation boundary, cooperative
cancellation, startup/close guard, root invalidation, result activation
containment recheck, asynchronous open, and one-based line navigation are
recorded by the parent source audit. The falsey non-callable cancellation
repair has a post-fix Aquinas/Luna source PASS and a direct source probe.

Cancellation remains cooperative and provider-specific; this does not claim
immediate interruption or crash-proof shutdown.

### D74-AC04 — packaged evidence

`REMAINS IN PROGRESS`. The current root/dist artifact is
`8B52D5209B6A080972D85F150595A3F85FF0CE54AA3EA7FA000E87E679A9AB7B` /
38,328,441 bytes, while the checked-in packaged search report has no artifact
identity and the prior interactive evidence binds older artifacts. Refreshing
those reports requires launching the software, prohibited by the current
project boundary. No report was rewritten or re-bound.

## Review and verification boundary

The historical Luna source review returned actionable findings that were
addressed, and the later Aquinas/Luna cancellation callback audit returned
PASS. The final broad D7.4 Luna/Terra review did not return a conclusion; no
child PASS is claimed for that unavailable review. This parent closure is
limited to the three criteria whose evidence is already source/offscreen
complete.

`scripts/check.ps1` and `scripts/verify_handoff.ps1` are required after the
handoff synchronization. No production source, test asset, or package input
changes in this slice.

The related S22 scenario projection is now recorded separately as
`accepted-with-limits` in `D7.4-s22-status-closure-parent-review.md`; this does
not promote D74-AC04, which remains `in-progress`.
