# UI-1.7 嵌入式调试工具站与白线修复交接

日期：2026-08-10

范围：只修改 `src/serialforge/presentation` 与交接文档；未触碰 application、domain、transport、protocol、recorder 或 ViewModel 契约。

## 本轮交付

- 为三个工作区滚动页建立 `settingsViewport` / `scrollViewport` / `scrollContent` 的稳定 presentation 标识，明确覆盖 viewport 的深色表面，修复原生 palette 白色背景风险。
- 修正 scroll content 的真实父子选择器，并将 workspace tab pane、tab、focus 样式限定在 `#workspaceTabs`。
- 补齐横向滚动条、横向滑块、两端按钮、分页区域和 `QAbstractScrollArea::corner` 的深色规则；这针对用户反馈的“整行白色”系统 fallback。
- 将导航与层级表达为“链路 / 连接、协议 / 遥测、命令管理”，主观测和发送区分别显示“实时观测”“发送控制”。
- 补齐暂停显示、UART 独占/DTR/RTS 的辅助信息；记录按钮统一为“开始/停止原始记录”。
- `StatusIndicator` 使用显式 `QPointF` 绘制浮点圆，鼠标事件透明；未新增 timer、线程、外部资源或业务依赖。

## 架构门检

架构审查确认依赖方向保持为 presentation → Qt/ViewModel，主题和装饰控件没有读取 payload、Session、transport 或 worker。

- 首轮架构师：`019fe790-2b37-7110-bc44-0c0f63732cf4`
- 增量门检：`019fe79e-e2fb-7ea3-9897-2673b4047fab`
- 白线修复门检：`019fe7a4-222c-7520-bb41-30aa50368a91`，结论 GO；仅提醒全局横向 scrollbar 规则会对所有 Qt 横向滚动条生效，与既有全局竖向规则对称，风险低。

## 静态证据

- `.venv\Scripts\ruff.exe check --no-cache src scripts`：`All checks passed!`
- `.venv\Scripts\ruff.exe format --check src scripts`：`55 files already formatted`
- AST：`parsed 55 files`
- 新进程导入 53 个 SerialForge 模块，未创建 `QApplication`。
- 主题字符串：`11535` chars；viewport、workspace tab、横向 scrollbar 和 corner 选择器存在；未解析 token 为 0。
- `src/serialforge/presentation` 外部图片、字体、GIF、QSS/QRC 文件：0；未新增资源或依赖。

## EXE 交付

- 根目录：[SerialForge.exe](../../SerialForge.exe)
- canonical：[dist/release/0.1.0/core/onefile/app/SerialForge.exe](../../dist/release/0.1.0/core/onefile/app/SerialForge.exe)
- 文件大小：`47,629,014` bytes
- SHA-256：`02A2DEB2E1DC6C6F3CAF1802B9307990AAA6B96FD62EC621AB0670B42B56E8E0`
- 根目录副本与 canonical 字节一致。
- provenance：`engineering_build`、`release_eligible=false`、`NotSigned`、`hardware_acceptance=not_run`、许可证 `inventory_only_pending_full_texts`。
- 打包命令：`pwsh.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\package.ps1 -Mode onefile`

## 未运行门禁

未启动 GUI/EXE、未做截图或 offscreen 渲染，未验证 Qt 实际像素、HIDPI、焦点/读屏、对比度、动效 CPU、真实 UART/网络/BLE/RTT 或硬件。当前结论是静态/打包 engineering build，不是启动通过或正式发行通过。

## 后续建议

如果用户授权一次性 GUI 验收，优先检查：工作区底部是否仍有白横线、最小窗口尺寸下 tab/横向 scrollbar 是否溢出、滚动条 corner 与终端/表格是否统一、读屏是否朗读暂停与记录状态。
