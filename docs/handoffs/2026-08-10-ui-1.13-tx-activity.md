# UI-1.13 发送成功信号反馈交接

日期：2026-08-10

## 本轮交付

- `_send_current()` 仅在 `SessionViewModel.send_entry()` 返回 `True` 后调用既有 `MotionController.request_activity()`。
- 顶部 SignalFieldWidget 现在会对成功发送产生短暂视觉反馈；RX 活动仍走原有 `_on_data_received()` 路径。
- 失败、格式错误、空 payload、回放、未连接、不可写会话和底层拒绝均不会触发成功动画。

## 架构边界

- 只修改 presentation 层单一发送入口；不修改 payload、ViewModel、application、transport、recorder、OTA 或 debug contract。
- 不新增控件、业务字段、timer 或第二个 motion clock；低动效/暂停/隐藏/最小化/关闭仍由 MotionController 统一降级。

## 验证与交付

- `scripts/check.ps1`：通过。
- Ruff format/check：通过。
- `python -m compileall -q src\serialforge`：通过。
- onefile provenance：通过；PyInstaller `6.22.0`、Python `3.12.13`。
- canonical/root EXE 字节一致：`47,633,043` bytes，SHA-256 `FA5D4BB6B3591D66535076B72B64C7A03B5D2D0D91CB339BEB8AEBDFB4606F56`。

## 未运行门

未启动 GUI/EXE，未做截图、HIDPI、焦点/读屏、动效性能和真实 UART/网络/BLE/RTT/J-Link/硬件验收；没有创建或运行测试专用资产。
