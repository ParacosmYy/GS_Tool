# UI-1.28 交接：协议流水线主题化轨道与共享动效

日期：2026-08-10  
范围：`pipelineSummary` presentation 装饰轨道、共享 MotionController 帧扇出、生命周期静态回退、最新 onefile EXE 交付  
父代理：Codex；父代理是本轮唯一写入者  
共享 checkout：`D:\Workplace\Agent_Workplace\SerialForge`；未检测到 `.git`，未创建/操作 worktree

## 实现

- 新增 `src/serialforge/presentation/pipeline_surface.py`：`PipelineSurfaceLabel(QLabel)` 在现有摘要标签底部安全
  padding 绘制四节点输入 → Protocol → Component → Dataset/Curve 轨道。
- 轨道只保存 `_phase`、`_animated`，通过 `set_frame(phase, animated)`/`stop()` 接收共享帧；只从
  `theme_spec_for_widget()` 读取当前主题 token，不读取 `state/source`，不创建 QTimer、不持有 MotionController、
  controller、ViewModel、domain、application 或 transport 依赖。
- `protocol.py` 仅替换构造类，保留 `ProtocolPanelWidgets.pipeline_summary`、`pipelineSummary` objectName、QSS
  selectors、动态属性、文本、tooltip 和 AccessibleDescription。
- `lifecycle.py` 在既有唯一帧扇出中加入一处 `set_frame()`，并在 reduced-motion、暂停、close、hide、minimize 分支加入
  `stop()`；不改变任何数据/连接/解析/回放语义。

## 架构与复核

架构师 `019fe8ea-e1e9-7992-bbe2-1f56157e1cf5`（Luna max/Fast）只读复核：Critical=0、条件 GO；要求保持 QLabel
契约、无状态源复制、无新时钟、主题上下文和五个 lifecycle 停止分支。独立质量复核
`019fe8f2-9072-7031-968e-83d8f5cd0607`（Luna max/Fast）：Critical=0、Required=0、GO；五轴均 GO。Optional
是后续可用 `contentsRect()` 代替固定底部 inset，当前不阻断交付。

## 验证

```text
scripts/check.ps1                              pass
107 Python files <= 1000 lines                 pass
theme token audit: 3 themes / 22 tokens        pass
ruff / compileall                              pass
UI1.28_PIPELINE_RESPONSIVE 980/1180            pass
UI1.28_PIPELINE_THEMES 3                      pass
UI1.28_PIPELINE_MOTION reduced/pause/hide       pass
UI1.28_PIPELINE_TEXT and accessibility          pass
no state/source/timer/controller in new widget  pass
```

截图：`build/ui_review_pipeline_980.png`、`build/ui_review_pipeline_1180.png`。中文方框是当前 offscreen 环境缺失
PySide6 fonts 的已知限制，不等同于 Windows 字体验收。没有创建/运行 unit test、mock、fixture、harness；没有启动持续 GUI、
HIDPI、EXE、真实读屏、性能、UART/TCP/UDP/BLE/RTT/J-Link 或硬件。

## EXE

- 根目录：[SerialForge.exe](../../SerialForge.exe)，已覆盖旧包。
- canonical：`dist/release/0.1.0/core/onefile/app/SerialForge.exe`
- SHA-256：`36CBDA2FACCA9736FB5C6891B7F9FFDA3F592DEBCD3CB1E1BC1CCA11D8427E1B`
- 大小：`47,740,933` bytes；canonical/root/manifest 三方一致。
- provenance：`NotSigned`、`release_eligible=false`、`hardware_acceptance=not_run`；Python `3.12.13`、PyInstaller `6.22.0`。

## 嵌入式适用性与后续

本轮没有嵌入式 C/C++、固件或 MCU 修改；embedded-enterprise-workflow 与 embedded-code-review-simplifier 的厂商
公共源适用性为 N/A。已记录独立复核、行为保持的简化评估和授权的静态/离屏非破坏性验证，不声称 MISRA、ISO 26262、
硬件、OTA 安全或正式发布合规。ARCH-6、ARCH-7b、ARCH-8，以及完整人工 GUI/HIDPI、EXE 启动和真实硬件验收继续保留。
