# UI-1.17 实时观测带状态表面交接

日期：2026-08-10  
范围：实时观测 station band 的历史、连接流程、暂停、记录、接收活动状态

## 结果

- `liveObservationBand` 增加 `state` 动态属性，状态优先级为 `history` → `transition` → `paused` → `recording/error` → `active` → `idle`。
- `_refresh_live_observation_state()` 只投影已有 `_history_*`、`SessionState`、`preview_paused`、`RecordingSnapshot` 和 `dataActivity` 事实；没有新增业务状态。
- QSS 增加 active/transition/paused/recording/error 深色表面，source=history 仍由既有历史色轴控制。
- 接收活动仍由共享 `MotionController` 驱动；helper 不创建 timer，也不读取 payload。

## 架构门与复核

- 架构师已调用：`019fe802-004b-7d30-88dd-62106ea7278d`（Luna max/Fast），超时关闭，未返回报告。
- 质量复核已调用：`019fe804-b959-7620-a78f-7f402fc3c680`（Luna max/Fast），超时关闭，未返回报告。
- 父代理完成静态五轴检查；窄窗口/实际动画性能待授权运行时验证。

## 验证

- `uv run ruff format src\\serialforge\\presentation\\main_window.py src\\serialforge\\presentation\\theme.py`：通过。
- `uv run ruff check src scripts`：通过。
- `python -m compileall -q src\\serialforge`：通过。
- `scripts/check.ps1`：通过。
- AST/导入审计：`parsed=66 imported=65`。
- 主题审计：`theme_chars=21834 white_backgrounds=0 widgets_motion_timers=1 selectors=pass`。
- 未运行 GUI/offscreen/截图/HIDPI/读屏/动效性能或 EXE 启动；依据当前约束未获运行时授权。

## 打包交付

- canonical：[dist/release/0.1.0/core/onefile/app/SerialForge.exe](../../dist/release/0.1.0/core/onefile/app/SerialForge.exe)
- 根目录覆盖副本：[SerialForge.exe](../../SerialForge.exe)
- 大小：`47,634,461` bytes
- SHA-256：`5D380B9AF29B5F16E72FE3FD31C48F93DDADE9F3E8862EC660CEE4193BC45C8C`
- canonical/root：字节一致
- provenance：`dist/release/0.1.0/core/onefile/PROVENANCE.json` verification 通过
