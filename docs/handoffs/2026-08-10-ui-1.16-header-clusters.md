# UI-1.16 顶栏状态与动效控制胶囊交接

日期：2026-08-10  
范围：Header 信息层级、状态动态表面、动效控制分组

## 结果

- `main_window.py` 将已有连接上下文、数据来源、状态灯和状态文字放入 `QFrame#statusCluster`。
- 两个已有动效开关放入 `QFrame#motionControls`，并补充容器级 AccessibleName/Description；开关本身的语义和槽函数不变。
- `_update_state_badge()` 复用 `SessionState` 给 `statusCluster` 设置动态属性，QSS 对 discovered/opening/open/closing/closed/error 提供暗色状态面。
- 没有新增业务状态、timer、线程、资源、依赖或 transport/OTA/debug 连接；`MotionController` 仍是唯一装饰共享时钟。

## 架构门与复核

- 架构师已调用：`019fe7fc-39a6-7180-a8f0-25a38bae894c`（Luna max/Fast），只读检查容器边界和动态属性；超时关闭，未返回报告，未作为通过证据。
- 质量复核已调用：`019fe7fe-b3a6-7b43-8c35-8cd3af32c04f`（Luna max/Fast）；超时关闭，父代理完成静态五轴审查。
- 父代理检查：初始化顺序安全、无跨层依赖、`widgets.py` 中 `QTimer(` 仍只有 1 处；窄窗口实际排版待授权运行时验证。

## 验证

- `uv run ruff format src\\serialforge\\presentation\\main_window.py src\\serialforge\\presentation\\theme.py`：通过。
- `uv run ruff check src scripts`：通过。
- `python -m compileall -q src\\serialforge`：通过。
- `scripts/check.ps1`：通过。
- AST/导入审计：`parsed=66 imported=65`。
- 主题审计：`theme_chars=20663 white_backgrounds=0 widgets_motion_timers=1 selectors=pass`。
- 未运行 GUI/offscreen/截图/HIDPI/读屏/动效性能或 EXE 启动；依据当前约束未获运行时授权。

## 打包交付

- canonical：[dist/release/0.1.0/core/onefile/app/SerialForge.exe](../../dist/release/0.1.0/core/onefile/app/SerialForge.exe)
- 根目录覆盖副本：[SerialForge.exe](../../SerialForge.exe)
- 大小：`47,634,839` bytes
- SHA-256：`4E39E2E97B5283D19DCF1A2FD7911A01B0C44A5A3EFD0641BBE249AC24F43064`
- canonical/root：字节一致
- provenance：`dist/release/0.1.0/core/onefile/PROVENANCE.json` verification 通过
