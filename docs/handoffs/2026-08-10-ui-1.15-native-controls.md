# UI-1.15 原生控件暗色兜底交接

日期：2026-08-10  
范围：ComboBox popup、Tab overflow button 和既有链路/发送状态带的主题回归保护

## 结果

- `src/serialforge/presentation/theme.py` 为 `QComboBox` 下拉箭头及 popup item 增加 hover/selected/disabled 暗色规则。
- `QTabWidget#workspaceTabs QTabBar QToolButton` 增加常态、hover、pressed、focus、disabled 规则，窄窗口 Tab 溢出时不会回落为系统白色按钮。
- 没有改动 ViewModel、domain、application、transport、OTA/debug contract 或动效时钟；变更保持 presentation-only。
- `connectionControlBand` 的链路链接行继续使用现有深色 palette 和状态属性，没有新增白色背景来源。

## 架构门

- 已调用架构师：`019fe7f3-569b-7ee2-b7a4-00d7d882ddc1`（Luna max/Fast）。该只读复核线程未返回报告后关闭；父代理依据 selector 作用域、presentation 边界和静态审计完成集成判断。

## 验证

- `uv run ruff format src\\serialforge\\presentation\\theme.py`：通过，文件未改变。
- `uv run ruff check src scripts`：通过。
- `python -m compileall -q src\\serialforge`：通过。
- `scripts/check.ps1`：通过。
- AST/导入审计：`parsed=66 imported=65`。
- 主题审计：`theme_chars=20010 white_backgrounds=0 selectors=pass`。
- 未运行 GUI/offscreen/截图/HIDPI/读屏/动效性能或 EXE 启动；依据当前约束未获运行时授权。

## 打包交付

- canonical：[dist/release/0.1.0/core/onefile/app/SerialForge.exe](../../dist/release/0.1.0/core/onefile/app/SerialForge.exe)
- 根目录覆盖副本：[SerialForge.exe](../../SerialForge.exe)
- 大小：`47,632,696` bytes
- SHA-256：`A21FA7365E3826DE8676EF585687B94AAE8C99374321F32937599E99A3B0BDF0`
- canonical/root：字节一致
- provenance：`dist/release/0.1.0/core/onefile/PROVENANCE.json` verification 通过
