# UI-1.29 交接：顶栏状态指示器几何与共享动效

日期：2026-08-10  
范围：`StatusIndicator` 六状态几何、主题 token 继承、共享 MotionController 动效、生命周期静态回退、最新 onefile EXE 交付  
父代理：Codex；父代理是本轮唯一写入者  
共享 checkout：`D:\Workplace\Agent_Workplace\SerialForge`；未检测到 `.git`，未创建/操作 worktree

## 实现

- 仅修改 `src/serialforge/presentation/widgets.py` 的 `StatusIndicator.paintEvent()` 和 Qt `QRectF`/角度常量导入。
- 保留 `_state`、`set_state()`、`set_frame()`、`stop()`、22×22 固定尺寸、透明鼠标和相邻可访问状态文字。
- `opening/closing` 绘制共享帧驱动旋转弧；`open` 绘制边界安全呼吸环；`discovered` 绘制四向定位角标；`error`
  绘制叉标；`closed` 保留静态环。颜色来自 `theme_spec_for_widget()`，无硬编码外部资源。
- 只使用已有 `_phase/_animated`，不新增 QTimer、业务字段、生命周期判断、ViewModel/domain/application/transport 依赖。
- `QRectF` inset 保证几何不越出 22×22，`drawArc` 使用命名的 Qt 1/16 度单位常量；低动效、暂停、隐藏、最小化、关闭
  仍由既有 MotionController/lifecycle 入口控制静态回退。

## 架构与复核

架构师 `019fe8fd-f9ff-7f30-bd29-ce2e3f826a8b`（Luna max/Fast）只读结论：有条件 GO、Critical=0；要求保持
state/frame/stop 契约、22×22 边界和静态回退。独立质量复核 `019fe903-bed0-7801-8761-76649234113c`（Luna max/Fast）
最终 GO：Critical=0、Required=0，五轴无阻塞项。

## 验证

```text
scripts/check.ps1                              pass
107 Python files <= 1000 lines                 pass
theme token audit: 3 themes / 22 tokens        pass
ruff / compileall                              pass
UI1.29_STATUS_FINAL 6 states × 3 themes        pass
UI1.29_STATUS_GEOMETRY 22x22 / arc units        pass
UI1.29_STATUS_MOTION reduced/pause/hide          pass
```

截图：`build/ui_review_status_indicator_1180.png`。offscreen 环境缺少 PySide6 fonts 目录，中文方框不等同于 Windows
字体验收失败；未运行持续 GUI、HIDPI、真实读屏、性能、EXE 启动、真实 UART/TCP/UDP/BLE/RTT/J-Link 或硬件。

## EXE

- 根目录：[SerialForge.exe](../../SerialForge.exe)，已覆盖旧包。
- canonical：`dist/release/0.1.0/core/onefile/app/SerialForge.exe`
- SHA-256：`93D56956EDB9C57E65934E0CDCF2DC4D09F594C95E3E7BFE45C1535B571738CF`
- 大小：`47,740,975` bytes；canonical/root/manifest 三方一致。
- provenance：`NotSigned`、`release_eligible=false`、`hardware_acceptance=not_run`；Python `3.12.13`、PyInstaller `6.22.0`。

## 嵌入式适用性与后续

本轮没有嵌入式 C/C++、固件或 MCU 修改；embedded-enterprise-workflow 与 embedded-code-review-simplifier 的厂商
公共源适用性为 N/A。已记录独立复核、行为保持的简化评估和授权的静态/离屏非破坏性验证，不声称 MISRA、ISO 26262、
硬件、OTA 安全或正式发布合规。ARCH-6、ARCH-7b、ARCH-8，以及完整人工 GUI/HIDPI、EXE 启动和真实硬件验收继续保留。
