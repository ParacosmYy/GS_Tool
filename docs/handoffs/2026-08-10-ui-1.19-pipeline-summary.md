# UI-1.19 协议流水线摘要状态表面交接

日期：2026-08-10  
范围：Protocol → Component → Dataset → Curve 摘要条的来源与状态视觉层级

## 结果

- `QLabel#pipelineSummary` 增加 `source=live/history` 与 `state=idle/active/transition/blocked/draft/history` presentation 属性。
- `_update_pipeline_summary()` 复用已有历史来源、`SessionState`、派生来源支持和协议编辑器 dirty 状态；没有新增业务状态或改变解析语义。
- 历史回放、连接流程、raw-only、未应用草稿、可派生实时会话和空闲态分别拥有暗色层级；所有分支明确指定背景、边框和左侧色带，修复协议摘要行可能出现的系统白色回落。
- 摘要文字、tooltip、AccessibleDescription 保持同一事实来源；颜色只做辅助信息。

## 架构门与复核

- 架构师已调用：`019fe80d-8676-7951-9880-cf7207324b5f`（Luna max/Fast），超时关闭，未返回报告。
- 质量复核已调用：`019fe80f-f2a5-7a73-ab75-d8a5cdc3f4a2`（Luna max/Fast），超时关闭，未返回报告。
- 父代理审查：状态优先级集中在一个展示 helper；QSS 只消费属性；动态属性沿用 `_set_dynamic_property()`，没有新 timer、资源或跨层依赖。

## 验证

- `uv run ruff format src\serialforge\presentation\main_window.py src\serialforge\presentation\theme.py`：通过。
- `uv run ruff check src scripts`：通过。
- `python -m compileall -q src\serialforge`：通过。
- `scripts/check.ps1`：通过。
- UI-1.19 静态审计：`AST main_window=ok theme=ok; theme_chars=23579; white_backgrounds=0; widgets_motion_timers=1; pipeline_selectors=6/6; pipeline_markers=3/3`。
- 未运行 GUI/offscreen/截图/HIDPI/读屏/动效性能或 EXE 启动；依据当前约束未获运行时授权。

## 打包交付

- canonical：[dist/release/0.1.0/core/onefile/app/SerialForge.exe](../../dist/release/0.1.0/core/onefile/app/SerialForge.exe)
- 根目录覆盖副本：[SerialForge.exe](../../SerialForge.exe)
- 大小：`47,635,597` bytes
- SHA-256：`23B86F5716BD50A0D8B99C98BEB4DB085856EB18E81A952E554EA1AAEC99214E`
- canonical/root：字节一致
- provenance：`dist/release/0.1.0/core/onefile/PROVENANCE.json` verification 通过
