# Handoff: 2026-08-09-d7-4-preview-boundary-fix

| Field | Value |
|---|---|
| ID | `2026-08-09-d7-4-preview-boundary-fix` |
| Delivery / slice | `D7 / D7.4 tab-expanded bounded preview fix` |
| Status | `accepted-with-limits` |
| Owner | `architect` |
| Checkout | Current local checkout only; no worktree |
| Created | `2026-08-09T22:49:46+08:00` |

## User outcome

Long Find in Files previews now preserve a visible trailing truncation marker
when tab expansion changes the displayed width, while remaining within the
configured preview bound. Search matches, file safety, cancellation, and UI
ownership are unchanged.

## Scope and boundaries

### In scope

- Correct `_preview()` truncation accounting after tab expansion.
- Small positive preview-limit behavior.
- Current package identity and evidence synchronization.

### Out of scope

- Search semantics, traversal, bounds, cancellation, stale-result handling,
  line navigation, or any new UI feature.
- Packaged interactive search, visual acceptance, cross-machine evidence,
  report regeneration, signing, installer, update, legal, or release gates.

## Team roles and ownership

| Role | Owner / agent | Responsibility in this handoff |
|---|---|---|
| Architect | Parent Codex | Integrate the smallest preview fix and final verification |
| Project Manager | Parent role record | Track D7.4 artifact and remaining AC04 gate |
| Product | Parent role record | Preserve bounded readable preview outcome |
| Developer 1 | Parent role record | Audit preview math and application boundary |
| Developer 2 | Parent role record | Package the source fix and synchronize identity |
| QA | Parent record; Luna no result | Read-only probe, static gate, and unrun runtime scope |

## Changed files and modules

- `src/quillforge/infrastructure/workspace_search_provider.py` — reserve
  ellipsis space against the expanded preview text.
- `docs/adr/0021-bounded-workspace-search.md` — record the preview contract.
- `docs/agent-team/reviews/D7.4-preview-boundary-parent-review.md` — record
  the finding, fix, and review outcome.
- `docs/agent-team/acceptance.json`, `docs/agent-team/delivery-register.json`,
  `docs/ROADMAP.md`, `docs/RELEASE_HANDOFF.md`, `docs/handoffs/index.json`,
  and this handoff — synchronize evidence and current artifact identity.
- `QuillForge.exe`, `dist/QuillForge.exe`, and
  `dist/QuillForge.release.json` — regenerated by the package owner script.

## Decisions and constraints

- Preview width is measured after tab expansion; marker characters count toward
  the configured maximum.
- No unit tests, mocks, fixtures, harnesses, or test-only assets were created
  or run.
- Shared-checkout writer: Architect only; Luna reviewer was read-only.
- Runtime launch policy: forbidden by the current project instruction.

## Verification commands and results

| Command / evidence | Result | Notes |
|---|---|---|
| Pure preview boundary source probe | `PASS` | Tab-expanded tail marker present; max lengths 10/2/1/20 respected |
| `scripts/check.ps1` | `PASS` | Formatting, compile, JSON, acceptance, and project checks passed |
| `scripts/package.ps1` | `PASS` | Root/dist synchronized at 38,328,894 bytes with current hash |
| Luna independent review | `NO RESULT` | Bounded window expired; no child PASS claimed |
| `scripts/verify_handoff.ps1` | `PASS` | Indexed handoff status matches Markdown status |
| `scripts/verify_release_handoff.ps1` | `EXPECTED NO-GO` | Current dossier reports ten open gates and the three mechanical stale-report failures `packaged_report_artifact_match`, `interactive_startup_report_consistent`, and `startup_preflight_report_consistent`; no launch was performed |

## Unrun checks and reason

- QuillForge.exe, QApplication, packaged `--diagnose-workspace-search`,
  interactive Find in Files, screenshots, DPI, and cross-machine search —
  prohibited by the active no-launch boundary.
- Current packaged search report regeneration — requires prohibited launch;
  historical report remains unmodified.
- Unit tests, mocks, fixtures, harnesses, and test-only assets — disallowed by
  project policy.

## Known risks and limits

- D7.4-AC04 remains `in-progress` because current packaged search/startup
  reports are not all bound to this artifact.
- The preview bound is a safety/display bound, not a large-file support range
  or performance SLA.
- The independent Luna review returned no conclusion; the parent source audit
  and probes do not substitute for unavailable runtime evidence.

## Assurance gate applicability

- Embedded enterprise workflow and embedded-code simplifier: **not
  applicable**. This slice changes Python/PyQt6 workspace-preview logic only;
  it contains no MCU, firmware, BSP/HAL/CMSIS, RTOS, ISR/DMA, or embedded
  C/C++ code.
- Public vendor-source applicability: none for the preview-boundary arithmetic;
  no manufacturer requirement or certification claim is inferred.
- Independent review: Luna was requested read-only and returned no result in
  the bounded window; the parent review is recorded in
  `docs/agent-team/reviews/D7.4-preview-boundary-parent-review.md`.
- Simplification assessment: the smallest behavior-preserving correction was
  retained; no unrelated abstraction or line-count refactor was justified.
- Authorized non-destructive validation: pure source probe, `scripts/check.ps1`,
  `scripts/package.ps1`, `scripts/verify_handoff.ps1`, and the expected no-go
  `scripts/verify_release_handoff.ps1` result. Launch, QApplication, target,
  and interactive validation remain prohibited and unrun.

## Acceptance and evidence IDs

- Acceptance: `D74-AC01`, `D74-AC02`, `S22`
- Evidence: `src/quillforge/infrastructure/workspace_search_provider.py`,
  `docs/adr/0021-bounded-workspace-search.md`,
  `docs/agent-team/reviews/D7.4-preview-boundary-parent-review.md`,
  `scripts/check.ps1`, `scripts/package.ps1`, current release manifest, and
  this handoff.

## Next owner and next action

- Owner: Architect / QA / Product.
- Action: retain D74-AC04 open until authorized current packaged report and
  interactive/cross-machine evidence are available.

## Artifact information

- Artifact path: `QuillForge.exe` / `dist/QuillForge.exe`
- Version: `0.1.0`
- SHA-256 / size: `E20913FBDF1FAE0DA7E30015D728FB3E0C7783BCB95CB01385FD44406B765F07` /
  `38,328,894` bytes; root and dist copies match
- Source snapshot: `tree-sha256:feed9088ad753fffd2e3d709a8af97af6255a882f59562ae29c2bc47904b91ee`
- Packaging note: rebuilt after the preview fix; release evidence is no-go
  until stale runtime reports are refreshed under authorized conditions.

## Disposition

`accepted-with-limits`: the preview fix is source- and package-verified; the
broader D7.4 packaged/runtime criterion remains open.
