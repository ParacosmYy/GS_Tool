# UI-1.18 发送状态胶囊交接

日期：2026-08-10  
范围：发送栏的可见状态反馈与暗色主题层级

## 结果

- `QLabel#sendState` 显示历史回放、处理中、可发送、输入内容、等待目标或等待连接。
- `_update_connection_controls()` 复用已有 `send_band_state` 和 `send_hint`；没有增加新的连接、BLE、TCP Server、批量或回放判断。
- 短文案用于快速扫描，完整 `send_hint` 继续写入 tooltip/AccessibleDescription；发送输入、模式、CRLF 和按钮的原有 gate 不变。
- 状态胶囊最大宽度为 132 px，避免窄窗口下过度挤压发送输入框；真实排版待授权 GUI/HIDPI 验收。

## 架构门与复核

- 架构师已调用：`019fe808-66fc-78e3-a0df-8d64e283eb68`（Luna max/Fast），超时关闭，未返回报告。
- 质量复核已调用：`019fe80a-722a-7813-ac6b-73ff5687c94e`（Luna max/Fast），超时关闭，未返回报告。
- 父代理完成静态五轴检查；无跨层依赖、无新计时器、无外部资源。

## 验证

- `uv run ruff format src\\serialforge\\presentation\\main_window.py src\\serialforge\\presentation\\theme.py`：通过。
- `uv run ruff check src scripts`：通过。
- `python -m compileall -q src\\serialforge`：通过。
- `scripts/check.ps1`：通过。
- AST/导入审计：`parsed=66 imported=65`。
- 主题审计：`theme_chars=22482 white_backgrounds=0 widgets_motion_timers=1 selectors=pass`。
- 额外静态顺序审计：`sendState` 初始化位于 `_build_send_bar`，更新只在 `_update_connection_controls`，无缺失 marker。
- 未运行 GUI/offscreen/截图/HIDPI/读屏/动效性能或 EXE 启动；依据当前约束未获运行时授权。

## 打包交付

- canonical：[dist/release/0.1.0/core/onefile/app/SerialForge.exe](../../dist/release/0.1.0/core/onefile/app/SerialForge.exe)
- 根目录覆盖副本：[SerialForge.exe](../../SerialForge.exe)
- 大小：`47,633,198` bytes
- SHA-256：`811A535D55D945C4385BC77F91127DC63FD49CC2BD21A8BB737BB198C68E3701`
- canonical/root：字节一致
- provenance：`dist/release/0.1.0/core/onefile/PROVENANCE.json` verification 通过
