# UI-1.32 交接：主题语义色点阵与共享动效

日期：2026-08-10  
范围：主题选择器旁的 presentation-only 视觉反馈。  
父代理：Codex；父代理是本轮唯一写入者。  
架构师：Luna max `019fe953-f5bd-7140-bbb5-8d58dc985204`，条件式 GO。  
独立复核：Luna max `019fe958-c7dd-78f3-9741-76cc6c0d5012`，GO，Critical=0、Required=0、Optional=1。  

## 变更

- 新增 `src/serialforge/presentation/theme_palette_surface.py:ThemePaletteSwatch`，固定 62×22 逻辑像素，绘制
  四个当前主题语义色点、连接轨道和共享 frame pulse。
- `controllers/workspace.py` 在 `themeControls` 中创建 `themePaletteSwatch`；不加入 `install_tab_order`，不设 role surface。
- `controllers/lifecycle.py` 将其加入既有 `_motion_surfaces()`，复用 `MotionController` 的 frame/stop fan-out。
- 不修改 `theme.py`、QSS、main_window/bootstrap/composition 或业务 controller；`apply_theme()` 已提供根属性和子控件 update。

## 架构边界

组件只依赖 `presentation/qt.py`、`theme_spec_for_widget()` 和标准库；不读业务状态、不创建 QTimer/主题 signal、不导入
ViewModel/domain/application/transport，不使用图片/字体/外部资源。它设置 NoFocus、WA_TransparentForMouseEvents 和空
accessibility，主题 combo 的文字和描述继续是唯一权威语义。绘制使用逻辑坐标和 Antialiasing，边界留 inset 以适配 HIDPI。

## 验证

```text
targeted py_compile / ruff                       pass
scripts/check.ps1                               pass (111 files <= 1000)
UI1.32_THEME_SWATCH                            pass (62x22, themes=3, 980/1180, hscroll=0)
UI1.32_MOTION_LIFECYCLE                         pass (reduced/pause/hide/close)
UI1.32_SCREENSHOT                               pass (build/ui_review_ui132_theme_swatch.png)
```

offscreen 环境报告 PySide6 fonts 目录缺失，中文可能显示方框；不将其等同于 Windows 运行时缺字。未创建/修改/运行 unit
test、mock、fixture、harness；未启动真实 GUI/EXE、HIDPI、读屏、键盘焦点、性能或 UART/TCP/UDP/BLE/RTT/J-Link 硬件。
该切片不涉及嵌入式 C/C++、固件或 MCU，embedded-enterprise-workflow 厂商源适用性为 N/A；不声称认证合规。

## 交付状态

UI-1.32 代码、静态/离屏验证、独立复核和 onefile 打包已完成，并已覆盖根目录副本。

```text
canonical/root byte match                          pass
SHA-256                                             FD07C6E9425F4524A912BFC000352963ECF28E3E3605F8028A8D3F2FD5C14CCC
size                                                47,758,161 bytes
manifest hash/size                                  pass
archive theme_palette/analysis_status/terminal      present
signature                                           NotSigned
release_eligible                                   false
hardware_acceptance                                not_run
```

canonical：`dist/release/0.1.0/core/onefile/app/SerialForge.exe`；根目录覆盖：[SerialForge.exe](../../SerialForge.exe)。
未启动 EXE 或进行正式 GUI/HIDPI/读屏/键盘焦点/真实硬件验收。

EMBEDDED_WORKFLOW_COMPLETE: review + simplification + verification recorded
