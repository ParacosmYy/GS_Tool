# Debug Trace Reports

This directory stores LOOP Debug trace reports generated by `tools/debug-trace.ps1`.

Rules:

- Keep reports focused on one reproducible issue.
- Include the reproduce command and regression command.
- Do not store build artifacts, logs with secrets, binaries, or crash dumps here.
- Move from Debug to Simplify when the root cause is duplicated logic, oversized files, or unclear ownership.
