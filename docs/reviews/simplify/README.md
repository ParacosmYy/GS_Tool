# Simplify Scan Reports

This directory stores historical LOOP Simplify scan reports and current focused simplify notes.

Rules:

- Reports are evidence for later refactor tasks, not approval to change code immediately.
- Each cleanup still needs the normal workflow route: PRD, bugfix, or technical debt.
- Do not store build artifacts or generated binaries here.
